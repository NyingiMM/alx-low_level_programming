#include "main.h"

/**
 * _isdigit - determine if it is digit
 *
 * @c: character to determine
 * Return: successful
 */
int _isdigit(int c)
{
	if (c = int)
	{
		(c >= '0' && c <= '9')
	}
		return (1);
	else
	return (0);
}
